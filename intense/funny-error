#include <stdio.h>
#include <stdlib.h>

int main(void) {
	char c;
	char* str = "";
	while(1) {
		c = getchar();

		if((int)c == 13 || (int)c == 3) {
			break;
		}
		str = malloc(sizeof(str) + sizeof(char));
		printf("\n\n%s", str);
	}
	return 0;
}
